# Crontab pour le nettoyage automatique de SEDI Tablette
# Exécuter toutes les heures pour nettoyer les données

# Nettoyage toutes les heures (minute 0 de chaque heure)
0 * * * * cd /path/to/tablettev2/backend && node scripts/auto-cleanup.js >> logs/cleanup.log 2>&1

# Nettoyage complet tous les jours à 2h du matin
0 2 * * * cd /path/to/tablettev2/backend && node scripts/auto-cleanup.js >> logs/cleanup-daily.log 2>&1

# Nettoyage des logs anciens (garder seulement 7 jours)
0 3 * * * find /path/to/tablettev2/backend/logs -name "*.log" -mtime +7 -delete
